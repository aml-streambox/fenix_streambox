#!/bin/bash

#= TVPro with Amlogic A311D2 SoC (T7 Platform)

SUPPORTED_UBOOT=("2019.01")
SUPPORTED_UBOOT_DESC=("U-boot 2019.01")
declare -A SUPPORTED_LINUX SUPPORTED_LINUX_DESC
SUPPORTED_LINUX["2019.01"]="5.15 7.1-rc1"
SUPPORTED_LINUX_DESC["2019.01"]="'Linux 5.15' 'Linux 7.1-rc1 local'"

DISTRIBUTION_ARRAY=("Ubuntu" "Debian")
DISTRIBUTION_ARRAY_DESC=("Ubuntu" "Debian")
Ubuntu_RELEASE_ARRAY=("jammy" "noble")
Ubuntu_RELEASE_VERSION_ARRAY=("22.04" "24.04")
Ubuntu_RELEASE_ARRAY_DESC=("Ubuntu 22.04" "Ubuntu 24.04")
Debian_RELEASE_ARRAY=("bullseye" "bookworm")
Debian_RELEASE_VERSION_ARRAY=("11" "12")
Debian_RELEASE_ARRAY_DESC=("Debian 11" "Debian 12")
DISTRIB_ARCH_ARRAY=("arm64")
Ubuntu_TYPE_ARRAY=("gnome" "server" "minimal")
Ubuntu_TYPE_ARRAY_DESC=("Desktop Image With GNOME Desktop"\
						"Headless Image With Essential Packages"\
						"Minimal Image With Very Basic Packages")
Debian_TYPE_ARRAY=("gnome" "server" "minimal")
Debian_TYPE_ARRAY_DESC=("Desktop Image With GNOME Desktop"\
						"Headless Image With Essential Packages"\
						"Minimal Image With Very Basic Packages")
INSTALL_TYPE_ARRAY=("EMMC" "SD-USB")
INSTALL_TYPE_ARRAY_DESC=("Image For Writing to eMMC Storage With USB Burning Tool"\
						 "Image For Writing to SD/USB Storage")

####################################
## Include VIM series common part
####################################
source config/boards/VIM.inc

case "$UBOOT" in
	2019.01)
		#UBOOT_DEFCONFIG="kvim4_defconfig"
		UBOOT_DEFCONFIG="${UBOOT_DEFCONFIG:-tvpro_defconfig}"
		UBOOT_GIT_BRANCH="${UBOOT_GIT_BRANCH:-main}"
		CROSS_COMPILE_PATH="$TOOLCHAINS/gcc-riscv-none-embed/bin/"
		export CROSS_COMPILE_PATH
		;;
	mainline)
		UBOOT_DEFCONFIG=""
		UBOOT_GIT_BRANCH="master"
		;;
esac

UBOOT_IMAGE_FILES="u-boot.bin.signed u-boot.bin.sd.bin.signed"
# Removed u-boot.bin.spi.bin.signed - board doesn't have SPI flash

case "$LINUX" in
	mainline)
		LINUX_DTB=""
		LINUX_GIT_BRANCH="master"
		LINUX_DEFCONFIG="${LINUX_DEFCONFIG:-defconfig}"
		SERIALCON="ttyAML0"
		GPU_VER=""
		GPU_PLATFORM=""
		GPU_TYPE=""
		MODESETTING_CONF=""
		;;
	5.15)
		# Multi-DTB: u-boot selects correct DTB at runtime based on DDR size and chip revision
		# All TVPRO DTBs are built, u-boot picks: t7_a311d2_tvpro{4g,8g}.dtb or t7c_a311d2_tvpro{4g,8g}.dtb
		LINUX_DTB="$LINUX_COMMON_DRIVERS_DIR/arch/arm64/boot/dts/amlogic/t7_a311d2_tvpro4gdebian.dtb"
		LINUX_GIT_BRANCH="${LINUX_GIT_BRANCH:-khadas-vims-5.15.y}"
		LINUX_DEFCONFIG="${LINUX_DEFCONFIG:-kvims_defconfig}"
		LINUX_DEFCONFIG_DIR="$LINUX_COMMON_DRIVERS_DIR/arch/arm64/configs"
		LINUX_COMMON_DRIVERS_GIT_BRANCH="${LINUX_COMMON_DRIVERS_GIT_BRANCH:-main}"
		LINUX_DEFCONFIG_FRAGMENT="${LINUX_DEFCONFIG_FRAGMENT:-tvpro-binder.config}"
		SERIALCON="ttyS0"
		GPU_VER="25p0"
		if [ "$DISTRIB_TYPE" != "server" ]; then
			GPU_PLATFORM="wayland"
		else
			GPU_PLATFORM="gbm"
		fi
		GPU_TYPE="gondul"
		MODESETTING_CONF=""
		;;
	7.1-rc1)
		if [ -z "$LINUX_SOURCE_URL" ] || [ "$LINUX_SOURCE_URL" == "https://github.com/khadas/linux" ]; then
			LINUX_SOURCE_URL="https://github.com/aml-streambox/linux.git"
		fi
		if [[ "$LINUX_SOURCE_URL" == /* ]]; then
			LINUX_VENDOR_DIR=$(realpath "$LINUX_SOURCE_URL")
		fi
		LINUX_DIR=$(realpath "$LINUX_VENDOR_DIR")
		LINUX_COMMON_DRIVERS_DIR="$LINUX_VENDOR_DIR/common_drivers"
		if [ -z "$LINUX_COMMON_DRIVERS_SOURCE_URL" ] || [ "$LINUX_COMMON_DRIVERS_SOURCE_URL" == "https://github.com/anshi233/common_drivers_fenix" ]; then
			LINUX_COMMON_DRIVERS_SOURCE_URL="https://github.com/aml-streambox/common_drivers.git"
		fi
		LINUX_DTB="$LINUX_COMMON_DRIVERS_DIR/arch/arm64/boot/dts/amlogic/amlogic-t7-a311d2-tvpro.dtb"
		LINUX_GIT_BRANCH="${LINUX_GIT_BRANCH:-v7.1-rc1-streambox}"
		LINUX_DEFCONFIG="${LINUX_DEFCONFIG:-defconfig}"
		LINUX_DEFCONFIG_DIR="$LINUX_DIR/arch/arm64/configs"
		LINUX_COMMON_DRIVERS_GIT_BRANCH="${LINUX_COMMON_DRIVERS_GIT_BRANCH:-v7.1-rc1-streambox}"
		LINUX_DEFCONFIG_FRAGMENT=""
		EXTRA_MAKE_ARGS="${EXTRA_MAKE_ARGS:+$EXTRA_MAKE_ARGS }COMMON_DRIVERS_DIR=common_drivers dtstree=common_drivers/arch/arm64/boot/dts"
		FENIX_KERNEL_DTB_TARGETS="amlogic/amlogic-t7-a311d2-tvpro.dtb amlogic/amlogic-t7-a311d2-khadas-vim4.dtb"
		SERIALCON="ttyAML0"
		GPU_VER=""
		GPU_PLATFORM=""
		GPU_TYPE=""
		MODESETTING_CONF=""
		SKIP_DT_OVERLAY_PACKAGE="yes"
		FENIX_SIMPLE_KERNEL_DEB="yes"
		FENIX_SIMPLE_DTB_ALIASES="t7c_a311d2_tvpro8gdebian.dtb"
		FENIX_SKIP_SELF_UPDATE="${FENIX_SKIP_SELF_UPDATE:-yes}"
		;;
esac

####
GPU_MODEL="arm-mali-bifrost-g52" # Mali G52 MP8(8EE)

CPUMIN=500000
CPUMAX=2208000
GOVERNOR=conservative

BOOTFS_TYPE="ext4"

BOOT_ENV_FILE=""
BOOT_SCRIPT_BOARD=""
BOOT_SCRIPT_VENDOR=""
BOOT_INI=""
BOOT_ENV_FILE_NEW="TVPRO_uEnv.txt"
[ "$LINUX" == "7.1-rc1" ] && BOOT_ENV_FILE_NEW="TVPRO_uEnv_7.1.txt"
BOOT_ENV_FILE_EXT=""
EXTLINUX_CONF="TVPRO_extlinux.conf"
[ "$LINUX" == "7.1-rc1" ] && EXTLINUX_CONF="TVPRO_extlinux_7.1.conf"
EXTLINUX_RECOVERY_CONF=""
[ "$LINUX" == "7.1-rc1" ] && EXTLINUX_RECOVERY_CONF="TVPRO_recovery_extlinux_7.1.conf"

#### Packing image
IMAGE_PACKAGE_CONF=package_t7.conf
IMAGE_PLATFORM_CONF=platform_t7.conf

####################################
uboot_custom_build() {
	# Cleanup old binaries
	info_msg "U-Boot custom build ..."
	rm -rf $UBOOT_IMAGE_DIR
	source fip/mk_script.sh tvpro $UBOOT_DIR
}

# Overwrite function in config/boards/VIM.inc
# Disable SPI flash postprocessing - board doesn't have SPI flash
# Armbian doesn't use this function either, and it works correctly
uboot_custom_postprocess() {
	# No-op: Board doesn't have SPI flash, so skip SPI flash image generation
	# The u-boot.bin.sd.bin.signed file from mk_script.sh is sufficient for eMMC/SD flashing
	info_msg "Skipping SPI flash postprocess (board has no SPI flash, using eMMC/SD only)"
	return 0
}

## Write u-boot
write_uboot_platform()
{
	dd if="$1/u-boot.bin.sd.bin.signed" of="$2" conv=fsync,notrunc bs=442 count=1 > /dev/null 2>&1
	dd if="$1/u-boot.bin.sd.bin.signed" of="$2" conv=fsync,notrunc bs=512 skip=1 seek=1 > /dev/null 2>&1
}

## Write u-boot for vendor u-boot
write_uboot_platform_ext()
{
	# Update u-boot partition
	[ -b /dev/mmcblk0p1 ] && dd if="$1/u-boot.bin.signed" of=/dev/mmcblk0p1 conv=fsync bs=512 seek=1 > /dev/null 2>&1
	# Enable write access for /dev/mmcblk0boot0
	[ -f /sys/block/mmcblk0boot0/force_ro ] && echo 0 > /sys/block/mmcblk0boot0/force_ro
	dd if="$1/u-boot.bin.signed" of=/dev/mmcblk0boot0 conv=fsync bs=512 seek=1 > /dev/null 2>&1
	# Enable write access for /dev/mmcblk0boot1
	[ -f /sys/block/mmcblk0boot1/force_ro ] && echo 0 > /sys/block/mmcblk0boot1/force_ro
	dd if="$1/u-boot.bin.signed" of=/dev/mmcblk0boot1 conv=fsync bs=512 seek=1 > /dev/null 2>&1
	# Disable write access for /dev/mmcblk0boot0/1
	[ -f /sys/block/mmcblk0boot0/force_ro ] && echo 1 > /sys/block/mmcblk0boot0/force_ro
	[ -f /sys/block/mmcblk0boot1/force_ro ] && echo 1 > /sys/block/mmcblk0boot1/force_ro
}

setup_write_uboot_platform()
{
	local tmp=$(cat /proc/cmdline)
	local bootsource="${tmp##*boot_source=}"
	local partition_type="${tmp##*partition_type=}"
	bootsource="${bootsource%% *}"
	partition_type="${partition_type%% *}"

	if [ "$bootsource" == "sd" ]; then
		DEVICE="/dev/mmcblk1"
	elif [ "$bootsource" == "emmc" ]; then
		if [ "$partition_type" == "generic" ]; then
			DEVICE="/dev/mmcblk0"
		else
			DEVICE="VENDOR"
		fi
	fi
}

## tweaks for platform
tweaks_platform() {
	echo "#Serial console allow root" >> $ROOTFS_TEMP/etc/securetty
	echo "ttyAML0" >> $ROOTFS_TEMP/etc/securetty

	# Setup default HDMI IN source
	echo "export HDMISRC=hdmi2" >> $ROOTFS_TEMP/etc/bash.bashrc
	# Setup GST environment
	echo "export GST_DEFAULT_V4L2_BUF_MODE=DMA_BUF_IMPORT" >> $ROOTFS_TEMP/etc/bash.bashrc

	# Enable services
	execute_in_chroot "systemctl --no-reload enable fan.service"

	# Enable binder service first (required by tvserver and audioserver)
	# Only enable application services when TVPRO_BUILD_APPS=yes
	if [ "${TVPRO_BUILD_APPS:-no}" == "yes" ]; then
		execute_in_chroot "systemctl --no-reload enable binder.service"

		# Enable tvserver and audioserver (depend on binder)
		execute_in_chroot "systemctl --no-reload enable tvserver.service"
		execute_in_chroot "systemctl --no-reload enable audioserver.service"
	fi

	if [ "$LINUX" != "mainline" ]; then
		if [ "$LINUX" != "7.1-rc1" ]; then
			execute_in_chroot "systemctl --no-reload enable bluetooth-khadas.service"
		fi
		execute_in_chroot "systemctl --no-reload enable usb-gadget-khadas.service"
	fi

	if [ "$DISTRIB_TYPE" != "server" ] && [ "$LINUX" != "mainline" ]; then
		execute_in_chroot "systemctl --no-reload enable panel.service"
		execute_in_chroot "systemctl --no-reload enable tone2-control.service"
	fi

	# Create boot links
	if [ "$INSTALL_TYPE" == "EMMC" ]; then
		execute_in_chroot "ln -fs /boot/Image Image"
		execute_in_chroot "ln -fs /boot/initrd.img initrd.img"
		execute_in_chroot "ln -fs /boot/dtb/$(basename $LINUX_DTB) dtb.img"
		execute_in_chroot "ln -fs /boot/dtb dtb"
	else
		# Setup default DTB for SD/USB images
		execute_in_chroot "ln -fs dtb/amlogic/$(basename $LINUX_DTB) /boot/dtb.img"
	fi

	if [ "$LINUX" != "mainline" ] && [ "$PANFROST_SUPPORT" == "yes" ]; then
		local overlay_env="$ROOTFS_TEMP/boot/dtb/${VENDOR,,}/$(basename $LINUX_DTB).overlay.env"
		sed -i --follow-symlinks '/fdt_overlays/s/=/=panfrost /g' $overlay_env
	fi

	# Configure GDM3 greeter dconf defaults for GNOME
	if [ "$DISTRIB_TYPE" == "gnome" ]; then
		local gdm_dconf_file="$ROOTFS_TEMP/etc/gdm3/greeter.dconf-defaults"
		if [ -f "$gdm_dconf_file" ]; then
			# Check if [org/gnome/settings-daemon/plugins/power] section exists
			if grep -q "^\[org/gnome/settings-daemon/plugins/power\]" "$gdm_dconf_file"; then
				# Section exists, update or add sleep-inactive-ac-type
				if grep -q "^sleep-inactive-ac-type=" "$gdm_dconf_file"; then
					# Update existing setting
					sed -i "s/^sleep-inactive-ac-type=.*/sleep-inactive-ac-type='nothing'/" "$gdm_dconf_file"
				else
					# Add setting after the section header
					sed -i "/^\[org\/gnome\/settings-daemon\/plugins\/power\]/a sleep-inactive-ac-type='nothing'" "$gdm_dconf_file"
				fi
			else
				# Section doesn't exist, add it with the setting
				echo "" >> "$gdm_dconf_file"
				echo "[org/gnome/settings-daemon/plugins/power]" >> "$gdm_dconf_file"
				echo "sleep-inactive-ac-type='nothing'" >> "$gdm_dconf_file"
			fi
		fi
	fi
}

## Build deb packages for platform
build_deb_packages_platform() {

	if  [ "$DISTRIB_RELEASE" == "noble" ] ; then
		info_msg "Building dracut_install package..."
		# FIXME
		# remove build stamp to force build for other arch
		rm -rf $BUILD/.stamps/dracut_install
		build_package "dracut_install:target"
	fi

	if [ "$DISTRIB_TYPE" == "minimal" ]; then
		warning_msg "Ignore build platform packages for minimal image."
		return 0
	fi

	if [ "$LINUX" == "5.15" ]; then
		if ([ "$DISTRIB_RELEASE" == "jammy" ] || [ "$DISTRIB_RELEASE" == "noble" ] || [ "$DISTRIB_RELEASE" == "bookworm" ]) && [ "$DISTRIB_TYPE" != "server" ]; then
			if [ "$PANFROST_SUPPORT" != "yes" ]; then
				info_msg "Building mali-debs package..."
				# FIXME
				# remove build stamp to force build for other arch
				rm -rf $BUILD/.stamps/mali-debs
				build_package "mali-debs:target"
			fi
		fi

		if  [ "$DISTRIB_RELEASE" == "noble" ] && [ "$DISTRIB_TYPE" != "server" ]; then
			if [ "$PANFROST_SUPPORT" != "yes" ]; then
				info_msg "Building wayland-debs package..."
				# FIXME
				# remove build stamp to force build for other arch
				rm -rf $BUILD/.stamps/wayland-debs
				build_package "wayland-debs:target"
			fi

			info_msg "Building chromium-debs package..."
			# FIXME
			# remove build stamp to force build for other arch
			rm -rf $BUILD/.stamps/chromium-debs
			build_package "chromium-debs:target"
		fi

		if [ "$DISTRIB_RELEASE" == "noble" ] && [ "$DISTRIB_TYPE" != "server" ]; then
			info_msg "Building blueman-deb package..."
			# FIXME
			# remove build stamp to force build for other arch
			rm -rf $BUILD/.stamps/blueman-deb
			build_package "blueman-deb:target"
		fi

		info_msg "Building OPTEE userspace package..."
		# FIXME
		# remove build stamp to force build for other arch
		rm -rf $BUILD/.stamps/optee_userspace_deb_aml
		build_package "optee_userspace_deb_aml:target"

		info_msg "Building OPTEE video firmware package..."
		# FIXME
		# remove build stamp to force build for other arch
		rm -rf $BUILD/.stamps/optee_video_firmware_deb_aml
		build_package "optee_video_firmware_deb_aml:target"

		info_msg "Building libion package..."
		# FIXME
		# remove build stamp to force build for other arch
		rm -rf $BUILD/.stamps/libion_deb_aml
		build_package "libion_deb_aml:target"

		info_msg "Building libge2d package..."
		# FIXME
		# remove build stamp to force build for other arch
		rm -rf $BUILD/.stamps/libge2d_deb_aml
		build_package "libge2d_deb_aml:target"

		info_msg "Building libmultienc  package..."
		# FIXME
		# remove build stamp to force build for other arch
		rm -rf $BUILD/.stamps/libmultienc_deb_aml
		build_package "libmultienc_deb_aml:target"

		info_msg "Building libjpegenc package..."
		# FIXME
		# remove build stamp to force build for other arch
		rm -rf $BUILD/.stamps/libjpegenc_deb_aml
		build_package "libjpegenc_deb_aml:target"

		# FIXME
		# remove build stamp to force build for other arch
		rm -rf $BUILD/.stamps/libamvenc_deb_aml
		build_package "libamvenc_deb_aml:target"

		info_msg "Building multimedia package..."
		# FIXME
		# remove build stamp to force build for other arch
		rm -rf $BUILD/.stamps/multimedia_debs_aml
		build_package "multimedia_debs_aml:target"

		if [ "$DISTRIB_RELEASE" == "jammy" ] || [ "$DISTRIB_RELEASE" == "noble" ]; then
			info_msg "Building gstreamer_aml package..."
			# FIXME
			# remove build stamp to force build for other arch
			rm -rf $BUILD/.stamps/gstreamer_aml
			build_package "gstreamer_aml:target"

			#info_msg "Building hdmiin-demo-debs-aml package..."
			# FIXME
			# remove build stamp to force build for other arch
			#rm -rf $BUILD/.stamps/hdmiin-demo-debs-aml
			#build_package "hdmiin-demo-debs-aml:target"
		fi

		# Audio / binder dependency chain
		# Build dependency packages first (boost, protobuf, grpc dev packages)
		info_msg "Building libboost1.83-dev package..."
		rm -rf $BUILD/.stamps/libboost1.83-dev
		build_package "libboost1.83-dev:target"

		info_msg "Building libprotobuf32t64 package..."
		rm -rf $BUILD/.stamps/libprotobuf32t64
		build_package "libprotobuf32t64:target"

		info_msg "Building protobuf-compiler package..."
		rm -rf $BUILD/.stamps/protobuf-compiler
		build_package "protobuf-compiler:target"

		info_msg "Building libgrpc-dev package..."
		rm -rf $BUILD/.stamps/libgrpc-dev
		build_package "libgrpc-dev:target"

		info_msg "Building libgrpc++-dev package..."
		rm -rf $BUILD/.stamps/libgrpc++-dev
		build_package "libgrpc++-dev:target"

		info_msg "Building android-liblog package..."
		rm -rf $BUILD/.stamps/android-liblog
		build_package "android-liblog:target"

		info_msg "Building android-binder package..."
		rm -rf $BUILD/.stamps/android-binder
		build_package "android-binder:target"

		info_msg "Building aml-audio-utils package..."
		rm -rf $BUILD/.stamps/aml-audio-utils
		build_package "aml-audio-utils:target"

		info_msg "Building aml-avsync package..."
		rm -rf $BUILD/.stamps/aml-avsync
		build_package "aml-avsync:target"

		info_msg "Building libexpat1 package..."
		rm -rf $BUILD/.stamps/libexpat1
		build_package "libexpat1:target"

		info_msg "Building aml-audio-hal package..."
		rm -rf $BUILD/.stamps/aml-audio-hal
		build_package "aml-audio-hal:target"

		info_msg "Building aml-audio-service package..."
		rm -rf $BUILD/.stamps/aml-audio-service
		build_package "aml-audio-service:target"

		# Build Amlogic U-Boot environment dev package (libubootenv.so + ubootenv.h)
		info_msg "Building aml-ubootenv-dev package..."
		rm -rf $BUILD/.stamps/aml-ubootenv-dev
		build_package "aml-ubootenv-dev:target"
		
		info_msg "Building libadla package..."
		# FIXME
		# remove build stamp to force build for other arch
		rm -rf $BUILD/.stamps/libadla_deb_aml
		build_package "libadla_deb_aml:target"

		info_msg "Building meson-display package..."
		# FIXME
		# remove build stamp to force build for other arch
		rm -rf $BUILD/.stamps/meson-display-deb-aml
		build_package "meson-display-deb-aml:target"

		info_msg "Building aml-tvserver-streambox package..."
		# FIXME
		# remove build stamp to force build for other arch
		rm -rf $BUILD/.stamps/aml-tvserver-streambox
		build_package "aml-tvserver-streambox:target"
	fi
}

## Install deb packages for platform
install_deb_packages_platform() {

	if [ "$DISTRIB_RELEASE" == "noble" ]; then
		info_msg "Installing dracut_install package ..."
		install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/$DISTRIBUTION-$DISTRIB_RELEASE/dracut_install/*.deb
	fi

	if [ "$DISTRIB_TYPE" == "minimal" ]; then
		warning_msg "Ignore install platform packages for minimal image."
		return 0
	fi

	if [ "$LINUX" == "5.15" ]; then
		if [ "$DISTRIB_RELEASE" == "noble" ] && [ "$DISTRIB_TYPE" != "server" ]; then
			if [ "$PANFROST_SUPPORT" != "yes" ]; then
				info_msg "Installing wayland-debs package ..."
				install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/wayland-debs/libwayland-client0_*.deb
				install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/wayland-debs/libwayland-server0_*.deb
				install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/wayland-debs/libwayland-cursor0_*.deb
				install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/wayland-debs/libwayland-egl1_*.deb
			fi

			info_msg "Installing chromium-debs package ..."
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/chromium-debs/chromium-common_*.deb
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/chromium-debs/chromium_*.deb
		fi

		if [ "$DISTRIB_RELEASE" == "noble" ] && [ "$DISTRIB_TYPE" != "server" ]; then
			info_msg "Installing blueman-deb package ..."
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/blueman-deb/blueman*.deb
		fi

		if ([ "$DISTRIB_RELEASE" == "jammy" ] || [ "$DISTRIB_RELEASE" == "noble" ] || [ "$DISTRIB_RELEASE" == "bookworm" ]) && [ "$DISTRIB_TYPE" != "server" ]; then
			if [ "$PANFROST_SUPPORT" != "yes" ]; then
				info_msg "Installing mali-debs package ..."
				install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/$DISTRIBUTION-$DISTRIB_RELEASE/mali-debs/${GPU_PLATFORM}/*.deb
			fi
		fi

		info_msg "Installing OPTEE userspace package..."
		install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/optee_userspace_deb_aml/*.deb

		info_msg "Installing OPTEE video firmware package..."
		install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/optee_video_firmware_deb_aml/*.deb

		info_msg "Installing libion package..."
		install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/libion_deb_aml/*.deb

		info_msg "Installing libge2d package..."
		install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/libge2d_deb_aml/*.deb

		info_msg "Installing libmultienc package..."
		install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/libmultienc_deb_aml/*.deb

		info_msg "Installing libjpegenc package..."
		install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/libjpegenc_deb_aml/*.deb

		info_msg "Installing libamvenc package..."
		install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/libamvenc_deb_aml/*.deb

		# Multimdia
		info_msg "Installing multimdia package..."
		install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/multimedia_debs_aml/gst-aml-drmbufferpool-plugins_*.deb
		install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/multimedia_debs_aml/mediactrlsrc_*.deb
		install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/multimedia_debs_aml/ispaml_*.deb

		if [ "$DISTRIB_RELEASE" == "jammy" ] || [ "$DISTRIB_RELEASE" == "noble" ]; then
			info_msg "Installing gstreamer_aml package..."
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/gstreamer_aml/gstreamer-aml_*.deb
			# gstreamer1.0
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/gstreamer_aml/gstreamer1.0-debs/libgstreamer1.0-0_*.deb
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/gstreamer_aml/gstreamer1.0-debs/gir1.2-gstreamer-1.0_*.deb
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/gstreamer_aml/gstreamer1.0-debs/gstreamer1.0-tools_*.deb
			# gst-plugins-base1.0
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/gstreamer_aml/gst-plugins-base1.0-debs/libgstreamer-plugins-base1.0-0_*.deb
			#install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/gstreamer_aml/gst-plugins-base1.0-debs/libgstreamer-gl1.0-0_*.deb
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/gstreamer_aml/gst-plugins-base1.0-debs/gstreamer1.0-plugins-base_*.deb
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/gstreamer_aml/gst-plugins-base1.0-debs/gstreamer1.0-plugins-base-apps_*.deb
			#install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/gstreamer_aml/gst-plugins-base1.0-debs/gstreamer1.0-x_*.deb
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/gstreamer_aml/gst-plugins-base1.0-debs/gstreamer1.0-alsa_*.deb
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/gstreamer_aml/gst-plugins-base1.0-debs/gir1.2-gst-plugins-base-1.0_*.deb
			# gst-plugins-good1.0
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/gstreamer_aml/gst-plugins-good1.0-debs/libgstreamer-plugins-good1.0-0_*.deb
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/gstreamer_aml/gst-plugins-good1.0-debs/gstreamer1.0-plugins-good_*.deb
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/gstreamer_aml/gst-plugins-good1.0-debs/gstreamer1.0-pulseaudio_*.deb
			# gst-plugins-bad1.0
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/gstreamer_aml/gst-plugins-bad1.0-debs/libgstreamer-plugins-bad1.0-0_*.deb
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/gstreamer_aml/gst-plugins-bad1.0-debs/gstreamer1.0-plugins-bad_*.deb

			#info_msg "Installing hdmiin-demo-debs-aml package..."
			#install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/hdmiin-demo-debs-aml/hdmiin-demo_*.deb
		fi

		# Audio / binder / application dependency chain
		# These are full applications (tvserver, audio service) and their build dependencies.
		# Per project requirements: "Do NOT port full applications — only kernel/userspace drivers"
		# Set TVPRO_BUILD_APPS=yes to include them (default: no).
		if [ "${TVPRO_BUILD_APPS:-no}" == "yes" ]; then
			info_msg "Installing libboost1.83-dev package..."
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/libboost1.83-dev/*.deb

			info_msg "Installing libprotobuf32t64 package..."
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/libprotobuf32t64/*.deb

			info_msg "Installing protobuf-compiler package..."
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/protobuf-compiler/*.deb

			info_msg "Installing libgrpc-dev package..."
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/libgrpc-dev/*.deb

			info_msg "Installing libgrpc++-dev package..."
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/libgrpc++-dev/*.deb

			# Install libexpat1 (runtime + headers combined)
			info_msg "Installing libexpat1 package (runtime + headers)..."
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/libexpat1/*.deb

			# Install android-binder first (it owns ashmem.h header)
			info_msg "Installing android-binder package..."
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/android-binder/android-binder_*_${DISTRIB_ARCH}.deb

			# Install android-liblog after android-binder (it no longer includes ashmem.h)
			info_msg "Installing android-liblog package..."
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/android-liblog/android-liblog_*_${DISTRIB_ARCH}.deb

			info_msg "Installing aml-audio-utils package..."
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/aml-audio-utils/aml-audio-utils_*_${DISTRIB_ARCH}.deb

			info_msg "Installing aml-avsync package..."
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/aml-avsync/aml-avsync_*_${DISTRIB_ARCH}.deb

			info_msg "Installing aml-audio-hal package..."
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/aml-audio-hal/aml-audio-hal_*_${DISTRIB_ARCH}.deb

			info_msg "Installing aml-audio-service package..."
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/aml-audio-service/aml-audio-service_*_${DISTRIB_ARCH}.deb

			# Install Amlogic U-Boot environment library/headers (used by aml-tvserver-streambox)
			if [ -n "$(ls $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/aml-ubootenv-dev 2>/dev/null)" ]; then
				info_msg "Installing aml-ubootenv-dev package..."
				install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/aml-ubootenv-dev/aml-ubootenv-dev_*_${DISTRIB_ARCH}.deb
			else
				warning_msg "aml-ubootenv-dev deb not found in build output; libubootenv.so may be missing from rootfs"
			fi

			info_msg "Installing aml-tvserver-streambox package..."
			install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/aml-tvserver-streambox/*.deb
		else
			info_msg "TVPRO_BUILD_APPS is not set to 'yes' — skipping full application packages (audio stack, tvserver, grpc, etc.)"
		fi

		# Userspace drivers / libraries (always installed)
		info_msg "Installing libadla package..."
		install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/libadla_deb_aml/*.deb

		info_msg "Installing meson-display package..."
		install_deb_chroot $BUILD_DEBS/$VERSION/$KHADAS_BOARD/${DISTRIBUTION}-${DISTRIB_RELEASE}/meson-display-deb-aml/*.deb
	fi
}
